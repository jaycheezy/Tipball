<%= form_for ([@team, @comment])  do |f| %>
	<% if @comment.errors.any? %>
	<h2><%= pluralize(@comment.errors.count, "error") %> prevent this post from saving:</h2>
	<ul>
		<% @comment.errors.full_messages.each do |msg| %>
		<li> <%= msg %></li>

		<% end %>
	<%end%>
	
	<%= f.label :text%><br>
	<%= f.text_area :text%>

	<br>

	<%= f.submit 'Post Comment'%>

<% end %>